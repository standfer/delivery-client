<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hello.maps1" >


    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACTION_CALL" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:persistent="true"
        android:theme="@style/AppTheme" >

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="google_api_key" />

        <service
            android:name="com.example.hello.maps1.services.TrackingService"
            android:enabled="true" >
        </service>

        <service
            android:name="com.example.hello.maps1.services.restarters.ForegroundService"
            android:enabled="true" >
        </service>

        <service
            android:name="com.example.hello.maps1.services.LocationUpdatesService"
            android:enabled="true"
            android:exported="true"/>

        <receiver
            android:name="com.example.hello.maps1.services.restarters.TrackingRestarterBroadcastReceiver"
            android:enabled="true"
            android:exported="true"
            android:label="RestartServiceWhenStopped">
        </receiver>

        <receiver
            android:name="com.example.hello.maps1.services.restarters.AlarmReceiver"
            android:enabled="true"
            android:exported="true"
            android:label="StartServiceByAlarm">
        </receiver>

        <activity
            android:name=".MainMapsActivity"
            android:launchMode="singleTop"
            android:persistent="true"
            android:label="@string/title_activity_main_maps" >
        </activity>

        <activity
            android:name=".LoginActivity"
            android:launchMode="singleTop"
            android:persistent="true"
            android:label="@string/title_activity_login" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RulesActivity"
            android:launchMode="singleTop"
            android:label="@string/title_activity_rules" >
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScheduleActivity"
            android:launchMode="singleTop"
            android:label="@string/title_activity_schedule" >
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StatisticsActivity"
            android:launchMode="singleTop"
            android:label="@string/title_activity_statistics" >
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:launchMode="singleTop"
            android:label="@string/title_activity_settings" >
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".OrderDetailsActivity"
            android:launchMode="singleTop"
            android:label="@string/title_activity_order_details" >
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <receiver android:name="com.example.hello.maps1.TimeNotification"></receiver>
        <service android:name="com.example.hello.maps1.MyIntentService" />
    </application>

</manifest>
